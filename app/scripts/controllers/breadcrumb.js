'use strict';

/**
 * @ngdoc function
 * @name quizApp.controller:BreadcrumbCtrl
 * @description
 * # BreadcrumbCtrl
 * Controller of the quizApp
 */
angular.module('quizApp')
    .controller('BreadcrumbCtrl', [
        '$scope',
        function($scope) {
            
        }
    ]);